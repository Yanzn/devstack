---
description: "Turn an idea into an approved written spec — socratic dialogue, assumption surfacing, section-by-section approval. Hands off to /plan."
---

Invoke the `devstack:flow/brainstorming` skill.

Follow its HARD-GATE: no code, no scaffolding, no implementation action until a written spec exists at `docs/devstack/specs/` and the user has approved it.

The terminal state of this command is invoking `devstack:flow/writing-plans`. Do not invoke any other implementation skill.
